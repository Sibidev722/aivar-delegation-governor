# Optional: Backend API URL (for production deployments, e.g. on Vercel)
# If left blank or omitted in local development, Vite proxies requests to http://127.0.0.1:8000 automatically.
VITE_API_URL=""
